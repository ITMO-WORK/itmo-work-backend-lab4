server:
  port: 0
spring:
  servlet:
    multipart:
      max-file-size: 200MB
  application:
    name: file-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: configserver:${CONFIG_SERVER_URL:http://localhost:9991}
